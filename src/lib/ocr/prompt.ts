export const OCR_SYSTEM_PROMPT = "";

export const OCR_USER_PROMPT = "Read only the numbered tables from this score sheet. Output each row as: number | number | number. Ignore all text, names, headers. Only output the numbers from table cells.";
